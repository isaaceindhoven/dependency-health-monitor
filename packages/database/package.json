{
  "name": "@dependency-health-monitor/database",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "type": "module",
  "scripts": {
    "push": "prisma db push",
    "push:acceptdataloss": "prisma db push --accept-data-loss",
    "push:reset": "prisma db push --force-reset",
    "generate": "prisma generate && tsc",
    "studio": "prisma studio",
    "dev": "npm run generate",
    "build": "npm run generate"
  },
  "dependencies": {
    "@prisma/client": "^3.15.2",
    "zod": "^3.17.10"
  },
  "devDependencies": {
    "esbuild": "^0.18.11",
    "prisma": "^3.15.2",
    "@frando/zod-prisma": "^0.5.7"
  }
}
